class Solution:
    def containsNearbyDuplicate(self, nums: List[int], k: int) -> bool:
        seen={}
        l=len(nums)
        for i in range(l):
            num=nums[i]
            if num in seen and abs(i-seen[num])<=k:
                return True
            seen[num]=i
        return False 
