class Solution:
    def isPalindrome(self, s: str) -> bool:
        s=s.lower()
        r=re.sub(r'[^A-Za-z0-9]','',s)
        if r==r[::-1]:
            return True
        else:
            return False
